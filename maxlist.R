# Create a list
my_list <- list(a = 10, b = 25, c = 5, d = 42, e = 18)

# Find min and max directly
cat("Minimum Value:", min(unlist(my_list)), "\n")
cat("Maximum Value:", max(unlist(my_list)), "\n")